org.gradle.jvmargs=-Xmx1280m -XX:MaxMetaspaceSize=256m -XX:ReservedCodeCacheSize=128m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Strict memory limits for low-RAM systems
org.gradle.workers.max=1
org.gradle.parallel=false

# Run Kotlin compiler in-process to avoid starting a separate Kotlin daemon process
kotlin.compiler.execution.strategy=in-process
